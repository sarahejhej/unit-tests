export const angleCalculator = turns => {
  const angle = 360;
  return angle * turns;
};

// should return 0 for 0 turns

// should return 360 for 1 turn

// should return 540 for 1.5 turns

// should return 720 for 2 turns
